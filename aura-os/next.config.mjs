/** Aura OS — configuration Next.js.
 *  Volontairement minimale : chaque option ajoutée ici est une option à
 *  comprendre plus tard. */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false
};
export default nextConfig;
