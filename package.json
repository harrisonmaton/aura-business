{
  "name": "aura-business",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "build:preview": "node scripts/build-preview.js",
    "test": "npm run build:preview && node tests/recette.js && node tests/registre-avec-runtime-simule.js"
  },
  "devDependencies": { "playwright": "^1.56.0" }
}
